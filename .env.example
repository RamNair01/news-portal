# Required for the News Assistant panel.
OPENAI_API_KEY=

# Optional: overrides the default gpt-5.6-terra model.
# OPENAI_MODEL=gpt-5.6-terra

# Optional: overrides the Nitter instance in config.yaml.
# NITTER_BASE_URL=https://your-nitter-instance.example
